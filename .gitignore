.worktrees/
.env

# docs/ tracks only the backend module documentation (docs/backend/) —
# everything else (planning specs, one-off plans, scratch notes) stays local
docs/*
!docs/backend
!docs/backend/**
.neon
.env.neon
node_modules/
